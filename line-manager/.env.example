# PanPay backend base URL (host -> published backend port, e.g. 8001)
PANPAY_URL=http://localhost:8000
# Platform ingest key (Admin → Settings → topup_ingest_key). Required.
PANPAY_INGEST_KEY=
# Optional: reconcile poll interval in ms (default 5000)
MANAGER_POLL_MS=5000
